# Configuration Backend
PORT=3011
NODE_ENV=production

# Sécurité - IMPORTANT: Changez cette valeur en production!
# Générez une clé aléatoire avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre-secret-jwt-super-securise-changez-moi

# Optionnel: Configuration de l'application
# MAX_FILE_SIZE=10485760  # 10MB
